package gametime;


/**
 * Write a description of class Runner here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class TestRunner
{
    //Mr.Wiebe's code - NO TOUCH ONLY LOOK
    public static void main(){
        // Donut donut = new Donut();
        // Adventurer person = new Adventurer("Megan");
        // Backpack myBackpack = person.getBackpack();
        // myBackpack.storeItem(donut);
        
        // Item item = myBackpack.useItem(donut);
        // if(item instanceof Food){
            
            // System.out.println( ((Food)donut).getHealth());
        // }
        // Item item2 = myBackpack.useItem(donut);
        // if(item2 instanceof Food){
            
            // System.out.println( ((Food)donut).getHealth());
        // }
    }
}
